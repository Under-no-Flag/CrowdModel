# 2026-06-22 日报

## 目标任务

为外滩有/无管控对比实验生成论文或汇报可用的 2 行 4 列高清密度热力图拼图，并记录复用命令。

## 已完成内容

- 新增 `codes/render_bund_comparison_panel.py`，支持输入某个 `bund_control_comparison_*` 目录和 4 个 step，读取 `controlled` 与 `uncontrolled` 两组 `fields/field_step_XXXX.npz` 数据，生成统一色条的 2x4 高清密度对比图。
- 脚本根据 `scene.toml` 中的 `[[walls]]` polyline 自动裁剪外滩主体区域，避免整张 512x224 网格带来过多留白；图中保留矢量墙线，并标注 `Step=...`、`Uncontrolled` 与 `Controlled`。
- 新增 `codes/tests/test_bund_comparison_panel.py`，覆盖 step 解析、缺失 step 处理、墙线裁剪和小型 fixture 出图。
- 在 `AGENT.md` 新增“外滩有/无管控密度热力图拼图”使用说明。
- 基于 `codes/results/bund_control_comparison_hcmbo` 生成示例图：
  - `codes/results/bund_control_comparison_hcmbo/figures/bund_comparison_density_panel_steps_0040_0400_0800_1590.png`

## 验证

- `python -m py_compile codes\render_bund_comparison_panel.py codes\tests\test_bund_comparison_panel.py`
- `python -m unittest codes.tests.test_bund_comparison_panel`
- 示例图尺寸检查结果为 `4729 x 1060`，文件大小约 `572 KB`。

## 目标任务

根据 `bund_control_comparison_hcmbo_capacity3` 实验结果更新论文第 5 章外滩补充实验数据。

## 已完成内容

- 更新 `writing/期刊论文章节/05-实验.md` 的 5.7 节，将有/无管控对比指标替换为 `codes/results/bund_control_comparison_hcmbo_capacity3` 的最终结果。
- 将管控方案表改为容量尺度系数 $3$ 下的有效入口通行速率上限，并保留实验通道与外滩 $3$ 至 $6$ 号通道的对应关系。
- 按论文指标编号规范，将通道负载均衡继续写作 $J_3$，避免使用代码历史字段中的旧 $J_5$。
- 在 5.7 节引用 `figures/bund_comparison_density_panel_steps_0050_0350_0900_1500.png`，补充有/无管控密度演化对比分析。

## 目标任务

将 Markdown 版 5.7 外滩场景 HCMBO 管控迁移验证转写到 IEEE LaTeX 稿件。

## 已完成内容

- 在 `writing/IEEE_lATEX/New_IEEEtran_how-to.tex` 的实验章节末尾、Discussion 之前新增 `Transfer Validation on a Bund-Inspired Scenario` 小节。
- 将外滩场景说明、有效入口通行速率上限表、有/无管控核心指标表、通道流量占比表和密度演化图转写为 IEEE LaTeX 格式。
- 保持 capacity scale 3 实验口径，并按论文规范将通道负载均衡写作 $J_3$。
- 补充转写中文 5.7 末尾结论，强调综合目标和通道负载均衡改善，同时说明未放行企图通量、离场吞吐和系统内剩余质量的代价。

## 验证

- 检查新增图件路径 `../图表/visio架构图/外滩场景示意图_页-7.jpg` 和 `../../codes/results/bund_control_comparison_hcmbo_capacity3/figures/bund_comparison_density_panel_steps_0050_0350_0900_1500.png` 均存在。
- 检查新增标签 `sec:bund_transfer`、`tab:bund_transfer_*`、`fig:bund_transfer_*` 已写入，Discussion 章节位置正常。

## 验证

- 检查 `05-实验.md` 中 5.7 节标题编号仍为 `5.7.1`、`5.7.2`，本章小结为 `5.8`。
- 检查旧版关键数值残留，未发现旧的 `0.6710`、`0.2255`、`6.4509`、`33.8%` 等 5.7 数据残留。

## 目标任务

根据 `writing/IEEE_lATEX/New_IEEEtran_how-to.tex` 与 `writing/审稿意见/审稿意见by claude and gpt.md`，整理审稿意见 response letter，不直接修改正文稿，并将文档保存到 `writing/审稿意见`。

## 已完成内容

- 新增 `writing/审稿意见/response_letter_codex_20260622.md`。
- 按 P0 意见逐条整理回复和拟修改说明，覆盖 $M(x;\eta_0)$ 含义、$U(x;s)$ 定义、Bellman--守恒律稳定性、入口限流质量守恒、指标编号、HCMBO 可复现细节、baseline 公平性、无量纲数字可读性、外滩迁移降调和硬安全约束。
- 将 P1/P2 建议合并为可执行补强项，包括 paired-seed 统计、权重敏感性、需求强度敏感性、质量闭合验证、运行时间和额外 baseline。
- 给出可直接迁移到正文的关键英文改写片段，包括 mobility tensor、目标标准化、外滩迁移解释和统计结论降调。

## 验证

- 检查 `response_letter_codex_20260622.md` 已保存于 `writing/审稿意见`。
- 检查回复信中包含 `mobility tensor`、`U(x;s)`、`CFL`、`mass balance`、`Algorithm 1`、`G6/G7-D`、`Bund`、`hard safety`、`paired-seed`、`weight sensitivity` 等关键审稿主题。
