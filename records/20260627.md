# 2026-06-27 审稿意见后续修改

针对 `writing/审稿意见/revision_feedback.md` 的后续条目，评估后选择性采纳并落地。

## 应做（已完成）
- **2.4 零流量**：外滩 Table VI（tab:bund_transfer_metrics）uncontrolled 组的 attempted/actual/unreleased 由 `0` 改为 `--`，表注说明 uncontrolled 未启用内部计量界面。
- **4 图标注**：容量响应图右轴 `(ped)`→`(ped-eq.)` 并重画（[g2_capacity_response_runner.py](codes/g2_capacity_response_runner.py)）；最优目标/收敛图坐标轴加 `(dimensionless)`（[final_hcmbo_cross_experiment_summary.py](codes/final_hcmbo_cross_experiment_summary.py)）并重画。
- **3.1 ρ_safe**：在 J2 定义后注明 `ρ_safe=3.5 ped/m²`，两场景一致（核对配置：g2 与 bund 均 3.5）。
- **κ 重载**：LCB 系数 κ→β（算法 REQUIRE 与采集函数）；`κ_{σ,r}` 注明量纲为 1/时间（代码 `transferable=dt*kappa*rho` 证实其为速率）；外滩 "κ=32" 改述为 `κ_{σ,r}=32`（仿真时间单位）。
- **3.2 q/A 单位**：内部入口容量节注明 A、q 物理单位为 ped/s。

## 推荐（已完成）
- **2.3 平均密度（省事版）**：Table VI 行名改为 "Domain-mean density (incl. empty space)"，表注说明为全域含空白平均，故远小于局部峰值。
- **3.3 量纲表**：新增 Table（tab:objective_units）列出 J1..J5 原始量纲与同量纲归一化分母，证明报告的 \tilde J_k 无量纲（J1:ped·s, J2:m²·s, J3:ped², J4:ped·s, J5:(ped/s)²）。

## 可选（已完成）
- **2.5 容量校验**：四通道设置补一句一致性校验（ρ_max×128×96≈6.1×10⁴ ped 为人数上界）。
- **5 术语**：实验节首次出现 channel 处补一句：channel=模型实体（索引 c），passage=外滩真实步道。

## 未采纳（评估后拒绝）
- 2.1 重新加入 "simulation mass unit" 描述句（会倒退已删的混淆概念）。
- 2.6 "全表统一 ped-eq"（真实人数 inflow/sink/system mass/actual 应为 ped；仅 attempted/unreleased 因重复积分用 ped-eq）。
- 5 引入新词 "walkway"（已有 walkable region/Ω_w，无需造词）。

PDF 两遍编译通过（16 页，无未定义引用）。
