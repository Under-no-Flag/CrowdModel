# 2026-06-29 Fig. 5 A/B panel regeneration

## 目标
定位 `Fig. 5` 的生成脚本并重新生成论文引用图，修复三个版面问题：
面板标注由 `A/B` 改为 `(a)/(b)`，提高 B 面板清晰度，并避免 B 左侧纵坐标被遮挡。

## 已完成
- 定位生成脚本为 `codes/crowd_bellman/g2_strategy.py`，论文引用图为 `writing/images/experiment_result_archive/g2_control_tradeoff_summary_ab.png`。
- 在 `g2_strategy.py` 中补回可复现的 `_save_control_tradeoff_summary_ab` 输出，使用已有 G2 结果摘要重绘 A/B 双面板图，不重新运行仿真。
- 将 `_ab` 图输出分辨率提升至 `3859x1786`，并通过更大的 B 面板比例、面板间距和上方标注位置避免纵坐标遮挡。
- 更新 `codes/crowd_bellman/redraw_paper_figures.py`，使后续 G2 论文图重绘会同步复制 `g2_control_tradeoff_summary_ab.png`。

## 验证
- `python -m py_compile codes/crowd_bellman/g2_strategy.py codes/crowd_bellman/redraw_paper_figures.py` 通过。
- 使用 `redraw_g2_figures()` 重新生成并复制论文图片。
- `pdflatex -interaction=nonstopmode -halt-on-error New_IEEEtran_how-to.tex` 连续两次编译通过，PDF 为 16 页。
- 已将 PDF 第 9 页转为 PNG 预览确认：Fig. 5 显示为 `(a)/(b)`，B 面板纵坐标未被遮挡。
