# 2026-04-20

## 任务目标
修复 IEEE 双栏排版中机制验证部分两张表格（Table I 与 Table II）在同页显示时互相覆盖的问题。

## 完成工作
- 修改文件：`writing/IEEE_lATEX/New_IEEEtran_how-to.tex`
- 针对 `tab:bidirectional` 与 `tab:sensitivity` 两张表：
  - 增加 `\setlength{\tabcolsep}{3pt}`，压缩列间距；
  - 增加 `\renewcommand{\arraystretch}{1.05}`，保持可读性；
  - 使用 `\resizebox{\columnwidth}{!}{...}` 将表格宽度约束到单栏宽度，避免跨栏溢出。
- 运行编译验证：
  - 命令：`pdflatex -interaction=nonstopmode -halt-on-error New_IEEEtran_how-to.tex`
  - 结果：编译成功，生成 PDF；表格不再重叠。
- 继续修复同一文档中的长公式溢出问题：
  - 将 `eq:subpop_bellman` 与 `eq:subpop_optimal` 改为 `aligned` 多行公式，避免单行公式超出 IEEE 单栏宽度；
  - 重新编译验证通过，PDF 成功生成。

## 产出物
- 更新后的 LaTeX 源文件：`writing/IEEE_lATEX/New_IEEEtran_how-to.tex`
- 新增任务记录：`records/20260420.md`
